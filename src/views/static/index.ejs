<!DOCTYPE html>
<html lang="en">
  <head>
      <% include ./partials/head.ejs %>
  </head>
  <body>
      <!-- Navigation -->
      <% include ./partials/navbar.ejs %>

      <!-- Page Content -->
      <main class="container">

        <% include ../static/partials/messages.ejs %>

        <!-- Jumbotron Header -->
        <header class="jumbotron my-4">
          <h1 class="display-3 text-center">musicXchange</h1>
          <p class="lead text-center">Rent instruments from local musicians.</p>
          <% if(!currentUser) { %>
            <div class="text-center">
              <a href="/users/sign_up" class="btn btn-success btn-lg">Sign up</a>
            </div>
          <% } %>
        </header>

        <!-- Page Features -->
        <% if(currentUser) { %>
          <div class="text-center">
            <a href="/cities" class="btn btn-warning btn-lg">Select Your City</a>
          </div>
        <% } %>


      </main>
      <!-- /.container -->

      <% include ./partials/baseScripts.ejs %>
    </body>
</html>
